{
  "name": "stik-product-page",
  "version": "1.0.0",
  "description": "My marvelous Nuxt.js project",
  "author": "eliteSufferer",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "npm run generate:gh-pages && npx gh-pages -d dist"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "nuxt-buefy": "^0.3.2"
  },
  "devDependencies": {}
}